[package]
name = "clamhub-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
clamhub-proto = { path = "../proto" }
tokio = { version = "1.0", features = ["full"] }
tonic = "0.11"
prost = "0.12"
tracing = "0.1"
tracing-subscriber = "0.3"
